server {
    listen 80;
    server_name django.sar-bc.ru;

    location /.well-known/acme-challenge/ {
        alias /var/www/certbot/.well-known/acme-challenge;  # Используем alias
        allow all;
    }

    location / {
        return 301 https://$host$request_uri;
    }
}
